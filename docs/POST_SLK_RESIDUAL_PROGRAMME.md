# BALANCE residual programme after SLK integration

The integrated flagship hierarchy is now canonical in [`zuizui0223/slk`](https://github.com/zuizui0223/slk).

SLK imports only the minimal BALANCE classification:

```text
L > 0 and Phi < 0
-> persistent compromise despite real conflict.
```

BALANCE remains canonical for identifying and quantifying the interior of that world.

## Primary residual paper

Working thesis:

**How to identify and quantify persistent compromise.**

Core claims to develop:

1. a middle world can be identified directly from optimized shared and differentiated-accessible worldlines;
2. `L>0` plus `Delta_W=W_D*-W_S*<0` does not require prior decomposition into `s` and `K`;
3. middle-world position, two-sided depth, reserve, width and connectedness are estimands in their own right;
4. the deepest BALANCE point is not generally the midpoint of the conflict-load interval;
5. direct and decomposed routes should be checked for concordance rather than averaged if they disagree.

## Secondary theoretical extension

Retain and develop:

- no-reentry and topology results;
- switching-cost hysteresis and historical persistence;
- finite-environment certification, including Lipschitz/covering or obstruction machinery where useful;
- functional-state versus structural-architecture claim levels;
- empirical worldline targets such as Pedicularis and observational critical-region anchors.

## Scope boundary after SLK

BALANCE should not re-present the full `L -> Phi -> accessibility -> invasion -> fixation -> occupancy` chain as its novelty. It should become the specialist paper on the geometry, identification, and persistence of the `L>0, Phi<0` domain.